# Dependencies
node_modules/

# Build output (rebuild with `npm run build`)
dist/
build/
*.tsbuildinfo

# Env & secrets (NEVER commit) — keep .env.example only
.env
.env.*
!.env.example

# Prisma
**/prisma/generated/

# Media storage (never committed)
storage/
uploads/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Reference-only clone of the Flutter mobile app (separate repo, not part of this project)
pixsign_repo/
